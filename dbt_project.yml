name: 'snowflake_utils'
version: '0.4.0'

config-version: 2

require-dbt-version: ">=1.0.0"

models-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_modules"]
test-paths: ["test"]
macro-paths: ["macros"]
log-path: "logs"
